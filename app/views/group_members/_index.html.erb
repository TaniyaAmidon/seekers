<h2 class="form-header">View the statuses of different group members</h2>


<div class="container">
  <div class="row">
    <div class="col-sm">
      <h3>Pending members</h3>
      <% pending_index.each do |member| %>
      <ul>
        <li><%= member.user.first_name %></li>
        <li><%= member.message_to_organiser %></li>
        <li><%= member.status %></li>
      </ul>

      <%= simple_form_for [@trip, member] do |f|%>

        <%= f.input :status, :as => :hidden, :input_html => { :value => "rejected" } %>

        <%= f.submit "reject", class: "btn btn-danger"%>

      <% end %>

      <%= simple_form_for [@trip, member] do |f|%>

        <%= f.input :status, :as => :hidden, :input_html => { :value => "accepted" } %>

        <%= f.submit "accept", class: "btn btn-success"%>

      <% end %>
      <% end %>
    </div>

    <div class="col-sm">
      <h3>Accepted members</h3>
      <% accepted_index.each do |member| %>
      <ul>
        <li><%= member.user.first_name %></li>
      </ul>
      <% end %>
    </div>

    <div class="col-sm">
      <h3>Rejected members</h3>
      <% rejected_index.each do |member| %>
      <ul>
        <li><%= member.user.first_name %></li>
      </ul>
      <% end %>
    </div>

  </div>
</div>
